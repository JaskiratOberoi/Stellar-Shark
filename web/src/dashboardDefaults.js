/** Labels must match `config/businessUnits.json` exactly. */
export const DEFAULT_VISIBLE_BU_LABELS = ['QUGEN', 'KARNAL', 'ROHTAK', 'Lucknow', 'SRINAGAR'];

/** LIS test codes matching `testCodePresets.json` entries for Vit D, B12, TSH, T3, T4. */
export const DEFAULT_VISIBLE_TEST_CODES = ['BI214', 'BI215', 'BI221', 'BI005', 'BI235'];
